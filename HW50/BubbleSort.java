import java.util.ArrayList;

public class BubbleSort {
    public static ArrayList sorted(ArrayList input) {
	boolean swaps = true;
	while (swaps) {
	    swaps = false;
	    for (int i = input.size() - 1; i <= 0; i--) {
		int x = input.
	    }
	}
    }
}
